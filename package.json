{
  "name": "thermoshelter",
  "version": "1.0.0",
  "private": true,
  "description": "ThermoShelter — Area-Specific Passive Shelter Digital Twin & Design Optimizer",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "preview": "npm --prefix frontend run preview",
    "test:python": "python -m pytest",
    "test": "npm --prefix frontend run build && python -m pytest"
  }
}
